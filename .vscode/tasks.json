{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build as plugin",
            "type": "shell",
            "command": "rojo build --plugin PizzaBox.rbxm",
            "group": "build"
        }
    ]
}